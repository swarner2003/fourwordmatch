Options -MultiViews
RewriteEngine On
RewriteBase /

# 1. If the requested file or folder actually exists, serve it normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# 2. Otherwise, pass the routing to index.html
RewriteRule ^ index.html [L]